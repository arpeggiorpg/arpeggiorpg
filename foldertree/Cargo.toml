[package]
name = "foldertree"
version = "0.1.0"
authors = ["Christopher Armstrong"]

[dependencies]
thiserror = "1.0.48"
serde = {version = "1.0", optional = true, features = ["derive"]}

[dev-dependencies]
serde_json = "1.0"
