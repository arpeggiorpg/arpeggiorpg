[package]
name = "foldertree"
version = "0.1.0"
authors = ["Christopher Armstrong"]
edition = "2021"

[dependencies]
thiserror = "1.0.48"
serde = {version = "1.0", optional = true, features = ["derive"]}
ts-rs = {version = "6.1"}

[dev-dependencies]
serde_json = "1.0"
