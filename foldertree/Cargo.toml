[package]
name = "foldertree"
version = "0.1.0"
authors = ["Christopher Armstrong"]
edition = "2021"

[dependencies]
thiserror = "1.0.48"
serde = {version = "1.0", optional = true, features = ["derive"]}
ts-rs = {version = "7.0.0"}

[dev-dependencies]
serde_json = "1.0"
